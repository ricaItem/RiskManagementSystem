@model WEB_Sentro.Areas.Vendor.Models.OrganizationsIndexViewModel
@{
    ViewData["Title"] = "Organization Management";
    Layout = "~/Areas/Vendor/Views/Shared/_LayoutVendor.cshtml";
}

<div class="space-y-8 animate-in fade-in duration-500">
    <div class="relative overflow-hidden rounded-3xl border border-slate-200/70 bg-white/50 p-8 shadow-sm backdrop-blur-md dark:border-slate-800/70 dark:bg-slate-900/50">
        <div class="relative z-10 flex flex-col gap-5 md:flex-row md:items-center md:justify-between">
            <div>
                <h1 class="text-2xl font-black tracking-tight text-slate-900 dark:text-white lg:text-3xl">
                    Client <span class="text-emerald-600 dark:text-emerald-400">Organizations</span>
                </h1>
                <p class="mt-2 max-w-2xl text-sm font-medium leading-relaxed text-slate-500 dark:text-slate-400">
                    Manage construction firm subscriptions, provision admin accounts, and monitor tenant health.
                </p>
            </div>
            <div class="flex items-center gap-3">
                <button class="flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-5 py-2.5 text-xs font-bold text-slate-700 transition-all hover:bg-slate-50 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-200">
                    Export Logs
                </button>
                <button onclick="toggleModal('provisionModal')" class="flex items-center gap-2 rounded-xl bg-emerald-600 px-5 py-2.5 text-xs font-bold text-white shadow-lg shadow-emerald-600/20 transition-all hover:bg-emerald-700">
                    <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M12 4v16m8-8H4" /></svg>
                    Provision New Firm
                </button>
            </div>
        </div>
    </div>

    <div class="grid gap-4 md:grid-cols-3">
        <div class="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900/50">
            <p class="text-[10px] font-black uppercase tracking-widest text-slate-400">Total Revenue</p>
            <p class="text-2xl font-black text-slate-900 dark:text-white">@Model.TotalRevenueDisplay</p>
        </div>
        <div class="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900/50">
            <p class="text-[10px] font-black uppercase tracking-widest text-slate-400">Active Tenants</p>
            <p class="text-2xl font-black text-emerald-600">@Model.ActiveTenantsCount Firms</p>
        </div>
        <div class="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900/50">
            <p class="text-[10px] font-black uppercase tracking-widest text-slate-400">System Availability</p>
            <p class="text-2xl font-black text-sky-500">@Model.SystemAvailabilityDisplay</p>
        </div>
    </div>

    <form method="get" class="grid gap-4 md:grid-cols-12">
        <div class="relative md:col-span-6 lg:col-span-8">
            <span class="absolute inset-y-0 left-4 flex items-center text-slate-400">
                <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /></svg>
            </span>
            <input type="text" name="search" value="@Model.Search" placeholder="Search by Firm Name or Code..." class="w-full rounded-2xl border border-slate-200 bg-white py-3 pl-12 pr-4 text-sm font-bold focus:border-emerald-500 focus:ring-0 dark:border-slate-800 dark:bg-slate-900/50 dark:text-white" />
        </div>
        <div class="md:col-span-3 lg:col-span-2">
            <select name="plan" class="w-full rounded-2xl border border-slate-200 bg-white py-3 px-4 text-sm font-bold dark:border-slate-800 dark:bg-slate-900/50 dark:text-white">
                <option value="">All Plans</option>
                <option value="Enterprise" @(Model.PlanFilter == "Enterprise" ? "selected" : "")>Enterprise</option>
                <option value="Professional" @(Model.PlanFilter == "Professional" ? "selected" : "")>Professional</option>
                <option value="Basic" @(Model.PlanFilter == "Basic" ? "selected" : "")>Basic</option>
            </select>
        </div>
        <div class="md:col-span-3 lg:col-span-2">
            <select name="status" class="w-full rounded-2xl border border-slate-200 bg-white py-3 px-4 text-sm font-bold dark:border-slate-800 dark:bg-slate-900/50 dark:text-white">
                <option value="">All Status</option>
                <option value="Active" @(Model.StatusFilter == "Active" ? "selected" : "")>Active</option>
                <option value="Pending" @(Model.StatusFilter == "Pending" ? "selected" : "")>Pending</option>
                <option value="Suspended" @(Model.StatusFilter == "Suspended" ? "selected" : "")>Suspended</option>
            </select>
        </div>
        <div class="md:col-span-12 flex justify-end">
            <button type="submit" class="rounded-xl bg-emerald-600 px-5 py-2.5 text-xs font-bold text-white">Apply filters</button>
        </div>
    </form>

    <div class="rounded-3xl border border-slate-200/70 bg-white shadow-sm dark:border-slate-800/70 dark:bg-slate-900/50 overflow-hidden">
        <div class="overflow-x-auto">
            <table class="w-full text-left border-collapse">
                <thead>
                    <tr class="border-b border-slate-100 dark:border-slate-800 bg-slate-50/50 dark:bg-slate-800/30">
                        <th class="px-6 py-4 text-[11px] font-black uppercase tracking-widest text-slate-400">Organization / Firm</th>
                        <th class="px-6 py-4 text-[11px] font-black uppercase tracking-widest text-slate-400">Plan</th>
                        <th class="px-6 py-4 text-[11px] font-black uppercase tracking-widest text-slate-400">Admins</th>
                        <th class="px-6 py-4 text-[11px] font-black uppercase tracking-widest text-slate-400">Risk Load</th>
                        <th class="px-6 py-4 text-[11px] font-black uppercase tracking-widest text-slate-400">Status</th>
                        <th class="px-6 py-4 text-[11px] font-black uppercase tracking-widest text-slate-400">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-100 dark:divide-slate-800">
                    @foreach (var org in Model.Organizations)
                    {
                        <tr class="group hover:bg-slate-50/50 dark:hover:bg-slate-800/20 transition-colors">
                            <td class="px-6 py-4">
                                <div class="flex items-center gap-3">
                                    <div class="h-10 w-10 rounded-xl bg-slate-100 dark:bg-slate-800 flex items-center justify-center font-bold text-slate-500">
                                        @(org.OrgName.Length > 0 ? org.OrgName[0].ToString().ToUpperInvariant() : "—")
                                    </div>
                                    <div>
                                        <div class="text-sm font-bold text-slate-900 dark:text-white">@org.OrgName</div>
                                        <div class="text-[10px] text-slate-500 font-medium">@org.OrgCode</div>
                                    </div>
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <span class="inline-flex items-center rounded-lg bg-slate-100 px-2.5 py-1 text-xs font-bold text-slate-600 dark:bg-slate-800 dark:text-slate-400">
                                    @org.PlanName
                                </span>
                            </td>
                            <td class="px-6 py-4 text-sm font-bold text-slate-600 dark:text-slate-400">
                                @org.AdminCount Account@(org.AdminCount != 1 ? "s" : "")
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex items-center gap-2">
                                    @if (org.RiskLoad.HasValue && org.RiskLoad > 0)
                                    {
                                        <div class="w-16 h-1.5 rounded-full bg-slate-100 dark:bg-slate-800 overflow-hidden">
                                            <div class="h-full bg-emerald-500" style="width: @(Math.Min(100, org.RiskLoad.Value / 4))%"></div>
                                        </div>
                                        <span class="text-xs font-bold text-slate-500">@org.RiskLoad</span>
                                    }
                                    else
                                    {
                                        <span class="text-xs font-bold text-slate-400">—</span>
                                    }
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex items-center gap-1.5">
                                    <span class="h-1.5 w-1.5 rounded-full bg-@(org.StatusColor)-500"></span>
                                    <span class="text-xs font-bold text-@(org.StatusColor)-600">@org.Status</span>
                                </div>
                            </td>
                            <td class="px-6 py-4">
                                <div class="flex gap-2">
                                    <button type="button" class="p-2 rounded-lg bg-slate-100 text-slate-500 hover:bg-emerald-500 hover:text-white transition-all dark:bg-slate-800">
                                        <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z" /></svg>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <div class="px-6 py-4 border-t border-slate-100 dark:border-slate-800 flex items-center justify-between">
            <span class="text-xs font-bold text-slate-500">Showing @Model.Organizations.Count of @Model.TotalCount Organizations</span>
        </div>
    </div>
</div>

<div id="provisionModal" class="fixed inset-0 z-50 hidden">
    <div class="absolute inset-0 bg-slate-900/40 backdrop-blur-sm dark:bg-[#020617]/80"></div>

    <div class="absolute inset-0 flex items-center justify-center p-4">
        <div class="w-full max-w-2xl transform overflow-hidden rounded-[2.5rem] border border-slate-200 bg-white p-10 shadow-2xl transition-all dark:border-slate-800 dark:bg-slate-900">

            <div class="flex items-center justify-between mb-8">
                <div>
                    <h2 class="text-2xl font-black text-slate-900 dark:text-white">Provision New <span class="text-emerald-600">Firm</span></h2>
                    <p class="text-sm font-medium text-slate-500">Set up a new tenant and create their first Main Admin account.</p>
                </div>
                <button onclick="toggleModal('provisionModal')" class="rounded-full p-2 text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800">
                    <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" /></svg>
                </button>
            </div>

            <form class="space-y-6">
                <div class="grid gap-6 md:grid-cols-2">
                    <div class="space-y-4">
                        <label class="block text-[10px] font-black uppercase tracking-widest text-slate-400">Organization Info</label>
                        <input type="text" placeholder="Construction Firm Name" class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold focus:border-emerald-500 focus:ring-0 dark:border-slate-800 dark:bg-slate-950 dark:text-white" />
                        <select class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-800 dark:bg-slate-950 dark:text-white">
                            <option>Select Subscription Plan</option>
                            <option>Basic (50 Risks)</option>
                            <option>Professional (200 Risks)</option>
                            <option>Enterprise (Unlimited)</option>
                        </select>
                    </div>

                    <div class="space-y-4">
                        <label class="block text-[10px] font-black uppercase tracking-widest text-slate-400">Primary Admin Account</label>
                        <input type="email" placeholder="Admin Email Address" class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-800 dark:bg-slate-950 dark:text-white" />
                        <input type="password" placeholder="Temporary Password" class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-800 dark:bg-slate-950 dark:text-white" />
                    </div>
                </div>

                <div class="rounded-2xl bg-emerald-50 p-4 dark:bg-emerald-900/10">
                    <div class="flex gap-3">
                        <svg class="h-5 w-5 text-emerald-600" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
                        <p class="text-xs font-medium text-emerald-800 dark:text-emerald-400">
                            Provisioning will automatically send a welcome email with onboarding instructions to the designated admin.
                        </p>
                    </div>
                </div>

                <div class="flex justify-end gap-3 pt-4">
                    <button type="button" onclick="toggleModal('provisionModal')" class="rounded-xl px-6 py-3 text-sm font-bold text-slate-500 hover:bg-slate-100 dark:hover:bg-slate-800">Cancel</button>
                    <button type="submit" class="rounded-xl bg-slate-900 px-8 py-3 text-sm font-black text-white hover:bg-slate-800 dark:bg-emerald-600 dark:hover:bg-emerald-500 shadow-xl shadow-emerald-600/10">Confirm & Provision</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        function toggleModal(id) {
            const modal = document.getElementById(id);
            modal.classList.toggle('hidden');
        }
    </script>
}