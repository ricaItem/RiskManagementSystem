@model IEnumerable<Web_Sentro.Areas.Client.Controllers.ArchivedEmployeeVm>
@{
    Layout = "_LayoutClient";
    ViewData["Title"] = "System Archive";
}

@if (TempData["Alert"] != null)
{
    var isSuccess = string.Equals(TempData["AlertType"]?.ToString(), "success", StringComparison.OrdinalIgnoreCase);
    var toastBg = isSuccess ? "bg-emerald-500" : "bg-rose-500";
    var toastIcon = isSuccess
        ? "<path d=\"M5 13l4 4L19 7\" stroke-width=\"3\" stroke-linecap=\"round\" stroke-linejoin=\"round\" />"
        : "<path stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"2\" d=\"M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z\" />";
    <div id="toastAlert" class="fixed top-6 right-6 z-[100] animate-in slide-in-from-right duration-500" role="alert">
        <div class="@toastBg text-white px-6 py-4 rounded-2xl shadow-2xl flex items-center gap-3">
            <svg class="w-5 h-5 shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">@Html.Raw(toastIcon)</svg>
            <span class="text-xs font-black uppercase tracking-widest">@TempData["Alert"]</span>
        </div>
    </div>
}

<div class="max-w-full mx-auto space-y-6 animate-in fade-in duration-700">
    <div class="flex justify-between items-end">
        <div>
            <h1 class="text-2xl font-black text-slate-900 dark:text-white uppercase tracking-tight">System <span class="text-rose-600">Archive</span></h1>
        </div>
        <div class="text-right">
            <span class="bg-rose-100 dark:bg-rose-900/30 text-rose-600 text-[10px] font-black px-3 py-1 rounded-full uppercase">
                @Model.Count() Archived Items
            </span>
        </div>
    </div>

    <div class="bg-white/50 dark:bg-slate-900/50 p-4 rounded-2xl border border-slate-200 dark:border-slate-800 backdrop-blur-md mb-6">
        <div class="grid grid-cols-1 md:grid-cols-12 gap-4">
            <div class="md:col-span-5 relative">
                <span class="absolute inset-y-0 left-0 pl-3 flex items-center text-slate-400">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" stroke-width="2.5" stroke-linecap="round" /></svg>
                </span>
                <input type="text" id="logSearch" placeholder="Search by Name, Email, Role..."
                       class="w-full bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl pl-10 pr-4 py-2.5 text-xs font-bold outline-none focus:ring-2 focus:ring-blue-500 transition-all">
            </div>

            <div class="md:col-span-3">
                <select id="severityFilter" class="w-full bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl px-4 py-2.5 text-xs font-bold outline-none focus:ring-2 focus:ring-blue-500">
                    <option value="">All Categories</option>
                    <option value="Employee">Employee</option>
                </select>
            </div>

            <div class="md:col-span-3">
                <input type="date" id="dateFilter" class="w-full bg-white dark:bg-slate-800 border border-slate-200 dark:border-slate-700 rounded-xl px-4 py-2.5 text-xs font-bold outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="md:col-span-1">
                <button onclick="resetLogs()" class="w-full h-full flex items-center justify-center bg-slate-100 dark:bg-slate-800 text-slate-500 rounded-xl hover:bg-slate-200 transition-all">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" /></svg>
                </button>
            </div>
        </div>
    </div>

    <div class="bg-white/80 dark:bg-slate-900/80 backdrop-blur-xl border border-slate-200 dark:border-slate-800 rounded-3xl overflow-hidden shadow-xl">
        <table class="w-full text-left">
            <thead>
                <tr class="text-[10px] font-black uppercase tracking-widest text-slate-400 border-b border-slate-100 dark:border-slate-800">
                    <th class="px-6 py-4">Reference</th>
                    <th class="px-6 py-4">Employee & Details</th>
                    <th class="px-6 py-4">Role</th>
                    <th class="px-6 py-4">Archived On</th>
                    <th class="px-6 py-4 text-right">Actions</th>
                </tr>
            </thead>
            <tbody class="divide-y divide-slate-50 dark:divide-slate-800/50">
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="5" class="px-6 py-20 text-center">
                            <div class="flex flex-col items-center opacity-20">
                                <svg class="w-12 h-12 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M5 8h14M5 8a2 2 0 110-4h14a2 2 0 110 4M5 8v10a2 2 0 002 2h10a2 2 0 002-2V8m-9 4h4" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" /></svg>
                                <p class="text-xs font-black uppercase tracking-widest">Archive is empty</p>
                            </div>
                        </td>
                    </tr>
                }
                @foreach (var item in Model)
                {
                    <tr class="archive-row hover:bg-rose-50/20 dark:hover:bg-rose-500/5 transition-all group" data-category="Employee">
                        <td class="px-6 py-5">
                            <div class="text-[10px] font-medium text-slate-400">@(item.UserId.Length > 8 ? item.UserId.Substring(0, 8) + "…" : item.UserId)</div>
                            <div class="text-[9px] font-medium uppercase px-2 py-0.5 rounded bg-slate-100 dark:bg-slate-800 w-fit">@item.Module</div>
                        </td>
                        <td class="px-6 py-5">
                            <div class="font-medium text-slate-900 dark:text-white text-xs">@item.Name</div>
                            <div class="text-[10px] font-medium text-slate-400">@item.Email</div>
                        </td>
                        <td class="px-6 py-5">
                            <div class="font-medium text-slate-700 dark:text-slate-300 text-[10px] uppercase">@item.Role</div>
                        </td>
                        <td class="px-6 py-5 text-[11px] font-bold text-rose-500/70">
                            @item.CreatedAt.ToString("dd MMM yyyy")
                        </td>
                        <td class="px-6 py-5 text-right">
                            <div class="flex justify-end gap-2">
                                <button type="button" class="btn-restore-archive bg-emerald-500/10 hover:bg-emerald-500 text-emerald-600 hover:text-white px-4 py-1.5 rounded-xl text-[10px] font-black uppercase transition-all"
                                        data-user-id="@item.UserId"
                                        data-user-name="@item.Name">
                                    Restore
                                </button>
                            </div>
                        </td>
                    </tr>
                }
                <tr id="noResultsRow" style="display: none;">
                    <td colspan="5" class="px-6 py-20 text-center">
                        <div class="flex flex-col items-center opacity-20">
                            <svg class="w-12 h-12 mb-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                            </svg>
                            <p class="text-xs font-black uppercase tracking-widest">No matching items found</p>
                        </div>
                    </td>
                </tr>
            </tbody>
        </table>
    </div>
</div>

<!-- Restore confirmation modal -->
<div id="restoreModalOverlay" class="fixed inset-0 bg-slate-900/60 backdrop-blur-sm z-[70] hidden opacity-0 transition-opacity duration-300 flex items-center justify-center p-4">
    <div class="bg-white dark:bg-slate-900 w-full max-w-md rounded-[2.5rem] shadow-2xl p-10 border border-slate-200 dark:border-slate-800">
        <h2 class="text-xl font-black text-slate-900 dark:text-white mb-2 uppercase">Restore <span class="text-emerald-600">Employee</span></h2>
        <p class="text-sm text-slate-500 dark:text-slate-400 mb-6">
            Restore <strong id="restoreModalName" class="text-slate-700 dark:text-slate-300"></strong>? They will reappear in Employee records with status <strong>Active</strong>.
        </p>
        <div class="flex gap-3">
            <button type="button" id="restoreModalCancel" class="flex-1 bg-slate-100 dark:bg-slate-800 text-slate-500 dark:text-slate-400 py-4 rounded-2xl font-black uppercase text-[10px] tracking-widest hover:bg-slate-200 dark:hover:bg-slate-700 transition-all">Cancel</button>
            <button type="button" id="restoreModalConfirm" class="flex-1 bg-emerald-600 text-white py-4 rounded-2xl font-black uppercase text-[10px] tracking-widest shadow-lg shadow-emerald-500/20 hover:bg-emerald-700 transition-all">Confirm Restore</button>
        </div>
    </div>
</div>

<!-- Hidden form for restore (submitted after confirmation) -->
<form id="restoreForm" action="@Url.Action("RestoreEmployee", "Archive", new { area = "Client" })" method="post" class="hidden">
    
    @Html.AntiForgeryToken()
    <input type="hidden" name="id" id="restoreEmployeeId" />
</form>

<script>
    // Toast auto-dismiss after 4 seconds
    (function () {
        const toast = document.getElementById('toastAlert');
        if (toast) {
            toast.style.transition = 'opacity 0.4s ease';
            setTimeout(function () {
                toast.style.opacity = '0';
                setTimeout(function () { toast.style.display = 'none'; }, 400);
            }, 4000);
        }
    })();

    document.addEventListener('DOMContentLoaded', function () {
        const searchInput = document.getElementById('logSearch');
        const categoryFilter = document.getElementById('severityFilter');
        const dateFilter = document.getElementById('dateFilter');
        const noResultsRow = document.getElementById('noResultsRow');

        const tableRows = document.querySelectorAll('tbody tr.archive-row');

        function filterTable() {
            const searchTerm = searchInput.value.toLowerCase();
            const selectedCategory = categoryFilter.value.toLowerCase();
            const selectedDate = dateFilter.value;

            let visibleCount = 0;

            tableRows.forEach(row => {
                const category = (row.getAttribute('data-category') || '').toLowerCase();
                const detailsCell = row.querySelector('td:nth-child(2)');
                const roleCell = row.querySelector('td:nth-child(3)');
                const searchableText = [detailsCell, roleCell].map(c => c ? c.textContent : '').join(' ').toLowerCase();
                const archivedOn = row.querySelector('td:nth-child(4)') ? row.querySelector('td:nth-child(4)').textContent.trim() : '';

                const matchesSearch = !searchTerm || searchableText.includes(searchTerm);
                const matchesCategory = !selectedCategory || category === selectedCategory;
                const matchesDate = !selectedDate || formatDateToMatch(archivedOn).includes(selectedDate);

                if (matchesSearch && matchesCategory && matchesDate) {
                    row.style.display = '';
                    visibleCount++;
                } else {
                    row.style.display = 'none';
                }
            });

            noResultsRow.style.display = (visibleCount === 0) ? '' : 'none';
        }

        function formatDateToMatch(dateString) {
            if (!dateString) return "";
            const date = new Date(dateString);
            return isNaN(date.getTime()) ? "" : date.toISOString().split('T')[0];
        }

        searchInput.addEventListener('input', filterTable);
        categoryFilter.addEventListener('change', filterTable);
        dateFilter.addEventListener('input', filterTable);
    });

    function resetLogs() {
        document.getElementById('logSearch').value = '';
        document.getElementById('severityFilter').selectedIndex = 0;
        document.getElementById('dateFilter').value = '';

        const event = new Event('input');
        document.getElementById('logSearch').dispatchEvent(event);
    }

    // Restore confirmation modal
    function openRestoreModal(userId, userName) {
        document.getElementById('restoreEmployeeId').value = userId;
        document.getElementById('restoreModalName').textContent = userName || 'this employee';
        const overlay = document.getElementById('restoreModalOverlay');
        overlay.classList.remove('hidden');
        setTimeout(function () { overlay.classList.add('opacity-100'); }, 10);
    }

    function closeRestoreModal() {
        const overlay = document.getElementById('restoreModalOverlay');
        overlay.classList.remove('opacity-100');
        setTimeout(function () { overlay.classList.add('hidden'); }, 300);
    }

    document.querySelectorAll('.btn-restore-archive').forEach(function (btn) {
        btn.addEventListener('click', function () {
            openRestoreModal(this.getAttribute('data-user-id'), this.getAttribute('data-user-name'));
        });
    });

    document.getElementById('restoreModalCancel').addEventListener('click', closeRestoreModal);
    document.getElementById('restoreModalConfirm').addEventListener('click', function () {
        document.getElementById('restoreForm').submit();
    });
</script>