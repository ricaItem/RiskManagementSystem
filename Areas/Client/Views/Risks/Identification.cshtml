@model Web_Sentro.Areas.Client.Models.PagedResult<Web_Sentro.Areas.Client.Models.RiskIdentificationViewModel>
@{
    Layout = "_LayoutClient";
    var isAdmin = ViewBag.IsAdmin == true;
    var isRiskManager = ViewBag.IsRiskManager == true;
    var query = Context.Request.Query;
    var baseUrl = Url.Action("Identification", "Risks", new { area = "Client", search = query["search"], status = query["status"], category = query["category"], siteId = query["siteId"], showDeleted = query["showDeleted"] });
    var siteFilterList = ViewBag.SiteFilterList as IEnumerable<Microsoft.AspNetCore.Mvc.Rendering.SelectListItem> ?? new List<Microsoft.AspNetCore.Mvc.Rendering.SelectListItem>();
    var sitesForRiskForm = ViewBag.SitesForRiskForm as IEnumerable<Microsoft.AspNetCore.Mvc.Rendering.SelectListItem> ?? new List<Microsoft.AspNetCore.Mvc.Rendering.SelectListItem>();
}

<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<style>
    .glass-card {
        background: var(--bg-card);
        border: 1px solid var(--border);
    }
    .dark .glass-card {
        background: rgba(15, 23, 42, 0.9);
        border: 1px solid rgba(255, 255, 255, 0.08);
    }
    .step-active {
        color: var(--brand-accent);
        border-bottom: 2px solid var(--brand-accent);
    }
</style>

<div class="animate-in fade-in min-h-screen space-y-8 duration-1000">
    @if (TempData["RejectError"] != null)
    {
        <div class="rounded-xl bg-rose-100 px-4 py-3 text-sm text-rose-700 dark:bg-rose-900/40 dark:text-rose-300">@TempData["RejectError"]</div>
    }
    <div class="flex flex-col justify-between gap-6 lg:flex-row lg:items-center">
        <div>
            <h1 class="text-3xl font-black tracking-tight text-slate-900 dark:text-white">
                Risk <span class="italic" style="color: var(--brand-accent);">Intelligence</span>
            </h1>
            <p class="mt-1 text-sm font-medium tracking-wide text-[var(--text-secondary)] dark:text-slate-500">Enterprise Threat Identification & Management</p>
        </div>

        <div class="flex items-center gap-3">
            <button onclick="toggleNewRiskModal()" class="btn-primary group relative flex items-center gap-3 rounded-xl px-6 py-3 shadow-card transition-all hover:-translate-y-0.5 hover:shadow-card-hover active:scale-95">
                <span class="absolute -top-1 -right-1 flex h-3 w-3">
                    <span class="absolute inline-flex h-full w-full animate-ping rounded-full opacity-75" style="background: var(--brand-accent);"></span>
                    <span class="relative inline-flex h-3 w-3 rounded-full" style="background: var(--brand-accent);"></span>
                </span>
                New Risk Entry
            </button>
        </div>
    </div>

    <div class="grid gap-4 md:grid-cols-2 lg:grid-cols-4">
        @foreach (var stat in new[] {
                new { Label = "Total Risks", Value = "12", Color = "emerald", Desc = "Active cases" },
                new { Label = "Uncategorized", Value = "03", Color = "amber", Desc = "Review needed" },
                new { Label = "Critical", Value = "05", Color = "rose", Desc = "Immediate action" },
                new { Label = "Site Pins", Value = "08", Color = "blue", Desc = "Geo-tagged" }
                })
        {
            <div class="kpi-card group rounded-xl sentro-card p-4 transition-all hover:-translate-y-0.5 hover:shadow-card-hover dark:border-slate-700 dark:bg-slate-800/90">
                <p class="font-black tracking-[0.2em] text-[10px] text-slate-400 uppercase">@stat.Label</p>
                <h3 class="mt-2 text-3xl font-black text-slate-900 dark:text-white">@stat.Value</h3>
                <p class="mt-2 w-fit rounded-md px-1.5 py-0.5 font-bold tracking-tighter text-[9px] uppercase @(stat.Color == "rose" ? "bg-rose-50 text-rose-500 dark:bg-rose-500/10" : stat.Color == "amber" ? "bg-amber-50 text-amber-600 dark:bg-amber-500/10" : stat.Color == "blue" ? "bg-sky-50 text-sky-600 dark:bg-sky-500/10" : "bg-emerald-50 text-emerald-600 dark:bg-emerald-500/10")">@stat.Desc</p>
            </div>
        }
    </div>

    <form method="get" action="@Url.Action("Identification", "Risks", new { area = "Client" })" id="filterForm" class="flex flex-wrap items-center gap-2 rounded-3xl sentro-card p-2 dark:border-slate-800 dark:bg-slate-900/90">
        <div class="flex min-w-[300px] flex-1 items-center rounded-2xl border border-transparent px-4 transition-all focus-within:ring-2 focus-within:ring-[var(--brand-primary)] focus-within:ring-opacity-20 dark:bg-slate-800/50" style="background: var(--bg-page);">
            <svg class="h-4 w-4 text-slate-400" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" stroke-width="3" stroke-linecap="round" /></svg>
            <input type="text" name="search" value="@Context.Request.Query["search"]" placeholder="Search global registry..." class="w-full bg-transparent p-4 text-xs font-bold outline-none dark:text-white">
        </div>
        @if (isAdmin)
        {
            <select name="status" id="statusFilter" class="cursor-pointer rounded-2xl border-none bg-white px-6 py-4 font-black tracking-widest text-[10px] uppercase shadow-sm outline-none dark:bg-slate-800 dark:text-slate-300">
                <option value="">All</option>
                <option value="NeedsAction">Needs Action</option>
                <option value="Draft" @(Context.Request.Query["status"] == "Draft" ? "selected" : "")>Draft</option>
                <option value="For_Review" @(Context.Request.Query["status"] == "For_Review" ? "selected" : "")>For review</option>
                <option value="Monitoring" @(Context.Request.Query["status"] == "Monitoring" ? "selected" : "")>Monitoring</option>
                <option value="MitigationRequired" @(Context.Request.Query["status"] == "MitigationRequired" ? "selected" : "")>Mitigation Required</option>
                <option value="Closed_Invalid" @(Context.Request.Query["status"] == "Closed_Invalid" ? "selected" : "")>Closed (Invalid)</option>
                <option value="Closed_Controlled" @(Context.Request.Query["status"] == "Closed_Controlled" ? "selected" : "")>Closed (Controlled)</option>
                <option value="Rejected" @(Context.Request.Query["status"] == "Rejected" ? "selected" : "")>Rejected</option>
            </select>
        }
        else
        {
            <select name="status" class="cursor-pointer rounded-2xl border-none bg-white px-6 py-4 font-black tracking-widest text-[10px] uppercase shadow-sm outline-none dark:bg-slate-800 dark:text-slate-300">
                <option value="">All Statuses</option>
                <option value="Draft" @(Context.Request.Query["status"] == "Draft" ? "selected" : "")>Draft</option>
                <option value="For_Review" @(Context.Request.Query["status"] == "For_Review" ? "selected" : "")>For review</option>
                <option value="Monitoring" @(Context.Request.Query["status"] == "Monitoring" ? "selected" : "")>Monitoring</option>
                <option value="MitigationRequired" @(Context.Request.Query["status"] == "MitigationRequired" ? "selected" : "")>Mitigation Required</option>
                <option value="Closed_Invalid" @(Context.Request.Query["status"] == "Closed_Invalid" ? "selected" : "")>Closed (Invalid)</option>
                <option value="Closed_Controlled" @(Context.Request.Query["status"] == "Closed_Controlled" ? "selected" : "")>Closed (Controlled)</option>
                <option value="Rejected" @(Context.Request.Query["status"] == "Rejected" ? "selected" : "")>Rejected</option>
            </select>
        }
        <select name="category" class="cursor-pointer rounded-2xl border-none bg-white px-6 py-4 font-black tracking-widest text-[10px] uppercase shadow-sm outline-none dark:bg-slate-800 dark:text-slate-300">
            <option value="">All Categories</option>
            <option value="Operational" @(Context.Request.Query["category"] == "Operational" ? "selected" : "")>Operational</option>
            <option value="Financial" @(Context.Request.Query["category"] == "Financial" ? "selected" : "")>Financial</option>
            <option value="Safety" @(Context.Request.Query["category"] == "Safety" ? "selected" : "")>Safety</option>
        </select>
        <select name="siteId" class="cursor-pointer rounded-2xl border-none bg-white px-6 py-4 font-black tracking-widest text-[10px] uppercase shadow-sm outline-none dark:bg-slate-800 dark:text-slate-300">
            @foreach (var item in siteFilterList)
            {
                <option value="@item.Value" selected="@(item.Value == ViewBag.SelectedSiteId?.ToString())">@item.Text</option>
            }
        </select>
        @if (isAdmin)
        {
            <label class="flex items-center gap-2 text-xs font-medium text-slate-600 dark:text-slate-400">
                <input type="checkbox" name="showDeleted" value="true" @(ViewBag.ShowDeleted == true ? "checked" : "") onchange="this.form.submit()" />
                Show deleted
            </label>
        }
        <button type="submit" class="btn-accent rounded-2xl px-8 py-4 text-sm font-semibold tracking-wide transition-all">
            Apply Filters
        </button>
    </form>

    <div class="rounded-xl sentro-card p-5 dark:border-slate-700 dark:bg-slate-800/90">
        <h3 class="mb-4 text-xs font-semibold tracking-wide text-[var(--text-primary)] dark:text-white">Risk Registry</h3>
        <div class="overflow-x-auto">
            <table class="w-full text-left" id="risksTable">
                <thead>
                    <tr class="border-b border-slate-100 font-black tracking-tighter text-[10px] text-slate-400 uppercase dark:border-slate-800">
                        <th class="pb-2 pr-4">Risk Identity</th>
                        <th class="pb-2 pr-4">Category</th>
                        <th class="pb-2 pr-4">Site</th>
                        <th class="pb-2 pr-4">Project Site</th>
                        <th class="pb-2 pr-4 text-center">Priority</th>
                        <th class="pb-2 pr-4 text-center">Status</th>
                        <th class="pb-2 pr-4">Date Reported</th>
                        <th class="pb-2 pr-4">Reported by</th>
                        <th class="w-48 pb-2 text-right">Actions</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-50 dark:divide-slate-800">
                    @foreach (var risk in Model.Items)
                    {
                        var status = risk.Status ?? "Draft";
                        var statusDisplay = status == "Rejected" ? "Rejected" : status == "For_Review" || status == "Submitted" || status == "Reviewed" ? "For review" : status == "Approved" ? "Monitoring" : status == "Closed_Invalid" ? "Closed_Invalid" : status;
                        var actionStatus = status == "Submitted" || status == "Reviewed" ? "For_Review" : status;
                        var dateReported = risk.DateReported ?? risk.DateLogged;
                        var reportedBy = risk.ReportedBy ?? risk.DetectedBy ?? "";
                        var isDeleted = risk.DeletedAt.HasValue;
                        var priorityVal = risk.Priority ?? "Unassessed";
                        var riskJson = System.Text.Json.JsonSerializer.Serialize(new {
                            risk.Id,
                            Title = risk.Title ?? "",
                            risk.Category,
                            Priority = priorityVal,
                            Status = statusDisplay,
                            risk.SourceType,
                            ProjectSite = risk.ProjectSite ?? "",
                            SiteId = risk.SiteId,
                            SiteName = risk.SiteName ?? "Unassigned",
                            DateReported = dateReported.ToString("yyyy-MM-dd"),
                            ReportedBy = reportedBy,
                            ReportByUserId = risk.ReportByUserId,
                            risk.OrgId,
                            DeletedAt = risk.DeletedAt,
                            Attachments = risk.Attachments ?? new List<string>()
                        });
                        <tr class="risk-row group cursor-default transition-all hover:bg-slate-50/50 dark:hover:bg-white/5 @(isDeleted ? "opacity-60" : "")" data-risk="@Html.Raw(System.Net.WebUtility.HtmlEncode(riskJson))" data-status="@statusDisplay" data-priority="@priorityVal" data-deleted="@(isDeleted ? "1" : "0")">
                            <td class="py-3 pr-4">
                                <div class="flex items-center gap-4">
                                    <div class="flex h-10 w-10 items-center justify-center rounded-2xl bg-emerald-500/10 text-xs font-black text-emerald-600">
                                        @(risk.Category?.Length > 0 ? risk.Category.Substring(0, 1) : "?")
                                    </div>
                                    <div>
                                        <div class="mb-1 max-w-[200px] truncate text-sm font-black tracking-tight text-slate-900 dark:text-white" title="@(risk.Title ?? "")">@(risk.Title ?? "")</div>
                                        @if (risk.AttachmentsCount > 0) { <span class="text-[10px] text-slate-400">@risk.AttachmentsCount file(s)</span> }
                                    </div>
                                </div>
                            </td>
                            <td class="py-3 pr-4">
                                <span class="rounded-lg bg-slate-100 px-2.5 py-1 font-medium text-slate-600 text-[10px] uppercase dark:bg-slate-800 dark:text-slate-400">@(risk.Category ?? "")</span>
                            </td>
                            <td class="py-3 pr-4 max-w-[120px] truncate font-medium text-[11px] text-slate-600 dark:text-slate-400" title="@(risk.SiteName ?? "Unassigned")">@(risk.SiteName ?? "Unassigned")</td>
                            <td class="py-3 pr-4">
                                <div class="flex max-w-[120px] items-center gap-2 truncate font-bold tracking-tighter text-[11px] text-slate-600 uppercase dark:text-slate-400" title="@(risk.ProjectSite ?? "")">
                                    <svg class="h-3 w-3 shrink-0 text-emerald-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" stroke-width="2" /></svg>
                                    @(risk.ProjectSite ?? "")
                                </div>
                            </td>
                            <td class="py-3 pr-4 text-center">
                                @{ var priorityCls = priorityVal == "Critical" ? "text-rose-500" : priorityVal == "High" ? "text-orange-500" : priorityVal == "Medium" ? "text-amber-500" : priorityVal == "Low" ? "text-emerald-500" : "text-slate-400"; }
                                <span class="rounded-lg px-3 py-1 font-black text-[10px] uppercase @(priorityVal == "Critical" ? "bg-rose-500/10 text-rose-500" : "bg-slate-100 dark:bg-slate-800 text-slate-500")">@priorityVal</span>
                            </td>
                            <td class="py-3 pr-4 text-center">
                                @{
                                    var statusBadgeClass = statusDisplay == "For review" ? "bg-blue-100 dark:bg-blue-900/40 text-blue-600 dark:text-blue-400" :
                                        statusDisplay == "Rejected" ? "bg-rose-100 dark:bg-rose-900/40 text-rose-600 dark:text-rose-400" :
                                        statusDisplay == "Monitoring" ? "bg-emerald-100 dark:bg-emerald-900/40 text-emerald-600 dark:text-emerald-400" :
                                        statusDisplay == "MitigationRequired" ? "bg-amber-100 dark:bg-amber-900/40 text-amber-600 dark:text-amber-400" :
                                        statusDisplay == "Closed_Invalid" ? "bg-rose-100 dark:bg-rose-900/40 text-rose-600 dark:text-rose-400" :
                                        statusDisplay == "Closed_Controlled" ? "bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400" :
                                        "bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400";
                                }
                                <span class="inline-flex items-center gap-1.5 rounded-lg px-2.5 py-1 @statusBadgeClass font-black tracking-widest text-[9px] uppercase">
                                    <span class="h-1.5 w-1.5 rounded-full @(statusDisplay == "For review" || statusDisplay == "Monitoring" ? "animate-pulse bg-current" : "bg-current")"></span>
                                    @statusDisplay
                                </span>
                            </td>
                            <td class="py-3 pr-4">
                                <span class="font-medium text-[10px] text-slate-500 uppercase dark:text-slate-400">@dateReported.ToString("MMM dd, yyyy")</span>
                            </td>
                            <td class="py-3 pr-4">
                                <span class="font-medium text-[10px] text-slate-600 dark:text-slate-300">@(reportedBy != "" ? reportedBy : "—")</span>
                            </td>
                            <td class="py-3 pr-4">
                                <div class="flex items-center justify-end gap-1">
                                    @if (!isDeleted)
                                    {
                                        <button type="button" onclick="openViewModal(this)" class="rounded-xl border border-slate-200 p-3 transition-all hover:bg-slate-800 hover:text-slate-100 dark:border-slate-700 dark:hover:bg-slate-700 dark:hover:text-slate-100" aria-label="View risk details">
                                            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" /><path stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" /></svg>
                                        </button>
                                        @if (actionStatus == "Draft" && (risk.ReportByUserId == ViewBag.CurrentUserId || isAdmin))
                                        {
                                            <form action="@Url.Action("HardDelete", "Risks", new { area = "Client" })" method="post" class="inline-block" onsubmit="return confirm('Are you sure you want to delete this?');">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="id" value="@risk.Id" />
                                                <input type="hidden" name="orgId" value="@risk.OrgId" />
                                                <button type="submit" class="rounded-xl border border-slate-200 p-3 text-rose-500 transition-all hover:bg-rose-50 dark:border-slate-700 dark:hover:bg-rose-900/20" aria-label="Delete permanently">
                                                    <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                                                </button>
                                            </form>
                                        }
                                        else if (actionStatus != "Draft")
                                        {
                                            <form action="@Url.Action("SoftDelete", "Risks", new { area = "Client" })" method="post" class="inline-block">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="id" value="@risk.Id" />
                                                <button type="submit" class="rounded-xl border border-slate-200 p-3 text-rose-500 transition-all hover:bg-rose-50 dark:border-slate-700 dark:hover:bg-rose-900/20" aria-label="Archive (soft delete)">
                                                    <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16" /></svg>
                                                </button>
                                            </form>
                                        }
                                    }
                                    else if (isAdmin)
                                    {
                                        <form action="@Url.Action("Restore", "Risks", new { area = "Client" })" method="post" class="inline-block">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@risk.Id" />
                                            <button type="submit" class="rounded-xl border border-slate-200 p-3 text-emerald-600 transition-all hover:bg-emerald-50 dark:border-slate-700 dark:hover:bg-emerald-900/20" aria-label="Restore risk"><svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" /></svg></button>
                                        </form>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        @{
            ViewData["PageNumber"] = Model.PageNumber;
            ViewData["TotalPages"] = Model.TotalPages;
            ViewData["TotalCount"] = Model.TotalCount;
            ViewData["PageSize"] = Model.PageSize;
            ViewData["BaseUrl"] = baseUrl;
        }
        @await Html.PartialAsync("_Pagination")
    </div>
</div>

<!-- New Risk Modal -->
<div id="newRiskModal" class="fixed inset-0 z-[60] hidden transition-all duration-300">
    <div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm" onclick="toggleNewRiskModal()"></div>
    <div class="relative flex min-h-screen items-center justify-center p-4">
        <div class="w-full max-w-xl scale-100 transform rounded-[2.5rem] border border-slate-200 bg-white p-6 shadow-2xl dark:border-slate-800 dark:bg-slate-900">
            <div class="mb-4 flex items-center justify-between">
                <h2 class="text-xl font-black text-slate-900 uppercase dark:text-white">Identify New Risk</h2>
                <button type="button" onclick="toggleNewRiskModal()" class="text-slate-400 hover:text-rose-500" aria-label="Close"><svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M6 18L18 6M6 6l12 12" /></svg></button>
            </div>
            <form action="@Url.Action("IdentifyNewRisk", "Risks", new { area = "Client" })" method="post" enctype="multipart/form-data" class="space-y-4" onsubmit="var btns=this.querySelectorAll('button[type=submit]');btns.forEach(function(b){b.disabled=true;});">
                @Html.AntiForgeryToken()
                <div class="grid grid-cols-2 gap-4">
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Date Reported</label>
                        <input type="date" name="ReportedDate" value="@DateTime.Now.ToString("yyyy-MM-dd")" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800" required />
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Risk Category</label>
                        <select name="Category" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:bg-slate-800">
                            <option>Operational</option>
                            <option>Financial</option>
                            <option>Safety</option>
                        </select>
                    </div>
                </div>
                <input type="text" name="Title" placeholder="Risk Description" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800" required />
                <div class="grid grid-cols-2 gap-4">
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Source Type</label>
                        <select name="SourceType" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:bg-slate-800">
                            <option value="Inspection">Inspection</option>
                            <option value="Incident">Incident</option>
                            <option value="Audit">Audit</option>
                            <option value="Observation">Observation</option>
                        </select>
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Status</label>
                        <input type="text" readonly value="Draft" class="w-full rounded-2xl border border-slate-200 bg-slate-100 px-4 py-3 text-sm font-bold text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-400" />
                    </div>
                </div>
                <div class="space-y-1">
                    <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Site</label>
                    <select name="SiteId" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800">
                        @foreach (var item in sitesForRiskForm)
                        {
                            <option value="@item.Value">@item.Text</option>
                        }
                    </select>
                </div>
                <div class="space-y-1">
                    <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Reported By</label>
                    <input type="text" readonly value="Auto-filled" class="w-full rounded-2xl border border-slate-200 bg-slate-100 px-4 py-3 text-sm font-bold text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-400" />
                </div>
                <div class="space-y-2">
                    <div class="relative">
                        <input type="text" id="addProjectSite" name="ProjectSite" placeholder="Job Site Location" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 pr-12 text-sm font-bold dark:border-slate-700 dark:bg-slate-800" required />
                        <button type="button" onclick="autoDetectLocation('addProjectSite', 'addMap')" class="absolute top-1/2 right-2 -translate-y-1/2 p-2 text-emerald-500 transition-transform hover:scale-110" aria-label="Detect location"><svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M15 10.5a3 3 0 11-6 0 3 3 0 016 0z" /><path stroke-width="2" d="M19.5 10.5c0 7.142-7.5 11.25-7.5 11.25S4.5 17.642 4.5 10.5a7.5 7.5 0 1115 0z" /></svg></button>
                    </div>
                    <div id="addMap" class="h-40 w-full overflow-hidden rounded-2xl border border-slate-200 dark:border-slate-700"></div>
                </div>
                <div class="space-y-1">
                    <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Attachments</label>
                    <input type="file" name="Attachments" multiple accept="image/jpeg,image/jpg,image/png,image/webp" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold file:mr-3 file:rounded-lg file:border-0 file:bg-emerald-50 file:px-4 file:py-2 file:text-xs file:font-bold file:text-emerald-700 dark:border-slate-700 dark:bg-slate-800 dark:file:bg-emerald-900/30 dark:file:text-emerald-300" />
                </div>
                @if (TempData["AttachmentError"] != null) { <p class="text-xs text-rose-600 dark:text-rose-400">@TempData["AttachmentError"]</p> }
                <div class="flex gap-3 pt-2">
                    <button type="submit" name="submitType" value="Draft" class="flex-1 rounded-2xl bg-slate-200 py-3.5 text-xs font-black tracking-widest text-slate-700 uppercase shadow disabled:opacity-70 dark:bg-slate-700 dark:text-slate-300">Save Draft</button>
                    <button type="submit" name="submitType" value="Submit" class="flex-1 rounded-2xl bg-emerald-500 py-3.5 text-xs font-black tracking-widest text-white uppercase shadow-lg disabled:opacity-70">Submit Risk</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- View Risk Modal -->
<div id="viewRiskModal" class="fixed inset-0 z-[60] hidden transition-all duration-300">
    <div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm" onclick="toggleViewModal()"></div>
    <div class="relative flex min-h-screen items-center justify-center p-4">
        <div class="w-full max-w-xl scale-100 transform rounded-[2.5rem] border border-slate-200 bg-white p-6 shadow-2xl dark:border-slate-800 dark:bg-slate-900">
            <div class="mb-4 flex items-center justify-between">
                <h2 class="text-xl font-black text-slate-900 uppercase dark:text-white">Risk <span class="text-emerald-500">Details</span></h2>
                <button type="button" onclick="toggleViewModal()" class="rounded-lg p-2 text-slate-400 transition-all hover:bg-slate-100 hover:text-rose-500 dark:hover:bg-slate-800" aria-label="Close"><svg class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M6 18L18 6M6 6l12 12" /></svg></button>
            </div>
            <div id="viewWorkflowStepper" class="mb-6 flex flex-wrap items-center justify-between gap-1 rounded-2xl bg-slate-100 px-2 py-3 font-bold text-[10px] uppercase dark:bg-slate-800/50">
                <span class="step" data-step="Draft">Draft</span>
                <span class="text-slate-400">→</span>
                <span class="step" data-step="Submitted">Submitted</span>
                <span class="text-slate-400">→</span>
                <span class="step" data-step="Assessed">Assessed</span>
                <span class="text-slate-400">/</span>
                <span class="step" data-step="MitigationRequired">Mitigation</span>
                <span class="text-slate-400">/</span>
                <span class="step" data-step="Monitoring">Monitoring</span>
                <span class="text-slate-400">/</span>
                <span class="step" data-step="Closed_Invalid">Closed</span>
            </div>
            <div id="viewRiskContent" class="space-y-4">
                <div class="space-y-1">
                    <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Title</label>
                    <div id="viewTitle" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div class="space-y-1">
                        <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Category</label>
                        <div id="viewCategory" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Priority</label>
                        <div id="viewPriority" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                    </div>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div class="space-y-1">
                        <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Status</label>
                        <div id="viewStatus" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Source Type</label>
                        <div id="viewSourceType" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                    </div>
                </div>
                <div class="space-y-1">
                    <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Site</label>
                    <div id="viewSiteName" class="min-h-[2.75rem] w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                </div>
                <div class="space-y-1">
                    <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Project/Site (location)</label>
                    <div id="viewProjectSite" class="min-h-[2.75rem] w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                </div>
                <div class="grid grid-cols-2 gap-4">
                    <div class="space-y-1">
                        <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Date Reported</label>
                        <div id="viewDateReported" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Reported By</label>
                        <div id="viewReportedBy" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-white"></div>
                    </div>
                </div>
                <div id="viewAttachmentsWrap" class="space-y-3">
                    <label class="ml-1 block font-black text-[10px] text-slate-400 uppercase">Attachments</label>
                    <div id="viewAttachmentsPreview" class="flex min-h-[160px] flex-col items-center justify-center rounded-2xl border border-slate-200 bg-slate-100 p-4 dark:border-slate-700 dark:bg-slate-800/50">
                        <!-- Filled by JS: centered image(s) or photo skeleton -->
                    </div>
                </div>
            </div>
            <div id="viewModalBottomBar" class="mt-4 flex hidden flex-wrap items-center justify-end gap-3 border-t border-slate-200 pt-4 dark:border-slate-700">
                <div id="viewModalActions" class="flex flex-wrap gap-3">
                    <form id="viewModalSubmitForm" action="@Url.Action("SubmitRisk", "Risks", new { area = "Client" })" method="post" class="hidden inline-flex">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="RiskId" id="viewModalSubmitRiskId" />
                        <button type="submit" class="inline-flex items-center gap-2 rounded-2xl bg-emerald-600 px-4 py-2.5 text-xs font-bold text-white uppercase shadow-md transition-all hover:bg-emerald-700">
                            <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M5 13l4 4L19 7" /></svg>
                            Submit
                        </button>
                    </form>
                    <a id="viewModalAssessLink" href="#" class="inline-flex items-center gap-2 rounded-2xl bg-amber-500 px-4 py-2.5 text-xs font-bold text-white uppercase shadow-md transition-all hover:bg-amber-600">
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2" /></svg>
                        Assess
                    </a>
                    <button type="button" id="viewModalRejectBtn" class="hidden inline-flex items-center gap-2 rounded-2xl bg-rose-600 px-4 py-2.5 text-xs font-bold text-white uppercase shadow-md transition-all hover:bg-rose-700">
                        <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M6 18L18 6M6 6l12 12" /></svg>
                        Reject
                    </button>
                </div>
                <div id="viewModalFooter" class="flex gap-3">
                    <button type="button" onclick="switchViewModalToEdit()" class="rounded-2xl bg-slate-900 px-5 py-2.5 text-xs font-black tracking-widest text-white uppercase shadow-lg dark:bg-white dark:text-slate-900">Edit</button>
                </div>
            </div>
            <div id="viewModalEditPanel" class="hidden space-y-4">
                <form id="viewModalEditForm" action="@Url.Action("UpdateRisk", "Risks", new { area = "Client" })" method="post" enctype="multipart/form-data" class="space-y-4">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="RiskId" id="viewEditRiskId" />
                    <div class="grid grid-cols-2 gap-4">
                        <div class="space-y-1">
                            <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Date Reported</label>
                            <input type="date" id="viewEditDate" name="ReportedDate" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800" required />
                        </div>
                        <div class="space-y-1">
                            <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Risk Category</label>
                            <select id="viewEditCategory" name="Category" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800">
                                <option value="Operational">Operational</option>
                                <option value="Financial">Financial</option>
                                <option value="Safety">Safety</option>
                            </select>
                        </div>
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Title</label>
                        <input type="text" id="viewEditTitle" name="Title" placeholder="Risk Description" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800" required />
                    </div>
                    <div class="grid grid-cols-2 gap-4">
                        <div class="space-y-1">
                            <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Source Type</label>
                            <select id="viewEditSourceType" name="SourceType" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800">
                                <option value="Inspection">Inspection</option>
                                <option value="Incident">Incident</option>
                                <option value="Audit">Audit</option>
                                <option value="Observation">Observation</option>
                            </select>
                        </div>
                        <div class="space-y-1">
                            <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Status</label>
                            <input type="text" id="viewEditStatus" readonly class="w-full rounded-2xl border border-slate-200 bg-slate-100 px-4 py-3 text-sm font-bold text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-400" />
                        </div>
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Reported By</label>
                        <input type="text" id="viewEditReportedBy" readonly class="w-full rounded-2xl border border-slate-200 bg-slate-100 px-4 py-3 text-sm font-bold text-slate-500 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-400" />
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Site</label>
                        <select id="viewEditSiteId" name="SiteId" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800">
                            @foreach (var item in sitesForRiskForm)
                            {
                                <option value="@item.Value">@item.Text</option>
                            }
                        </select>
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Project/Site (location)</label>
                        <input type="text" id="viewEditProjectSite" name="ProjectSite" placeholder="Job Site Location" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800" required />
                    </div>
                    <div class="space-y-1">
                        <label class="ml-1 font-black text-[10px] text-slate-400 uppercase">Attachments</label>
                        <input type="file" name="Attachments" multiple accept="image/*" class="w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold file:mr-3 file:rounded-lg file:border-0 file:bg-emerald-50 file:px-4 file:py-2 file:text-xs file:font-bold file:text-emerald-700 dark:border-slate-700 dark:bg-slate-800 dark:file:bg-emerald-900/30 dark:file:text-emerald-300" />
                    </div>
                    <div class="flex gap-3 pt-2">
                        <button type="button" onclick="switchViewModalToView()" class="flex-1 rounded-2xl bg-slate-100 py-3.5 text-xs font-black text-slate-500 uppercase dark:bg-slate-800 dark:text-slate-400">Cancel</button>
                        <button type="submit" class="flex-[2] rounded-2xl bg-slate-900 py-3.5 text-xs font-black text-white uppercase shadow-lg dark:bg-white dark:text-slate-900">Save Changes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<!-- Reject Modal -->
<div id="rejectModal" class="fixed inset-0 z-[70] hidden transition-all duration-300">
    <div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm" onclick="closeRejectModal()"></div>
    <div class="relative flex min-h-screen items-center justify-center p-4">
        <div class="w-full max-w-md rounded-[2.5rem] border border-slate-200 bg-white p-6 shadow-2xl dark:border-slate-800 dark:bg-slate-900">
            <h3 class="mb-4 text-lg font-black text-slate-900 dark:text-white">Reject Risk</h3>
            <p class="mb-4 text-sm text-slate-500 dark:text-slate-400">Remarks are required when rejecting.</p>
            <form action="@Url.Action("Reject", "Risks", new { area = "Client" })" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" id="rejectRiskId" />
                <textarea name="RejectRemarks" id="rejectRemarks" rows="3" class="mb-4 w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm font-bold dark:border-slate-700 dark:bg-slate-800 dark:text-white" placeholder="Enter remarks..." required></textarea>
                <div class="flex gap-3">
                    <button type="button" onclick="closeRejectModal()" class="flex-1 rounded-2xl bg-slate-200 py-2.5 text-xs font-bold text-slate-600 uppercase dark:bg-slate-700 dark:text-slate-300">Cancel</button>
                    <button type="submit" class="flex-1 rounded-2xl bg-rose-600 py-2.5 text-xs font-bold text-white uppercase">Reject</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Hard Delete Modal -->
<div id="hardDeleteModal" class="fixed inset-0 z-[70] hidden transition-all duration-300">
    <div class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm" onclick="closeHardDeleteModal()"></div>
    <div class="relative flex min-h-screen items-center justify-center p-4">
        <div class="w-full max-w-md rounded-[2.5rem] border border-slate-200 bg-white p-6 shadow-2xl dark:border-slate-800 dark:bg-slate-900">
            <h3 class="mb-2 text-lg font-black text-rose-600 dark:text-rose-400">Permanently delete risk?</h3>
            <p class="mb-4 text-sm text-slate-500 dark:text-slate-400">This will delete the risk, all attachments, and evaluations. This action cannot be undone.</p>
            <form action="@Url.Action("HardDelete", "Risks", new { area = "Client" })" method="post" onsubmit="return confirm('Are you sure you want to permanently delete this risk?');">
                @Html.AntiForgeryToken()
                <input type="hidden" name="id" id="hardDeleteRiskId" />
                <input type="hidden" name="orgId" id="hardDeleteOrgId" />
                <div class="flex gap-3">
                    <button type="button" onclick="closeHardDeleteModal()" class="flex-1 rounded-2xl bg-slate-200 py-2.5 text-xs font-bold text-slate-600 uppercase dark:bg-slate-700 dark:text-slate-300">Cancel</button>
                    <button type="submit" class="flex-1 rounded-2xl bg-rose-600 py-2.5 text-xs font-bold text-white uppercase">Delete permanently</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
<script>
(function() {
    const assessBaseUrl = '@Url.Action("Assess", "Risks", new { area = "Client" })';
    const isAdmin = @(isAdmin ? "true" : "false");
    const isRiskManager = @(isRiskManager ? "true" : "false");
    const currentUserId = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(ViewBag.CurrentUserId ?? ""));
    const isEmployeeOnly = @((ViewBag.IsEmployeeOnly ?? true) ? "true" : "false");

    let _addMapInstance = null;
    let _editMapInstance = null;

    window.toggleNewRiskModal = function() {
        const m = document.getElementById('newRiskModal');
        m.classList.toggle('hidden');
        document.body.style.overflow = m.classList.contains('hidden') ? 'auto' : 'hidden';
    };

    window.toggleViewModal = function() {
        const m = document.getElementById('viewRiskModal');
        m.classList.add('hidden');
        document.body.style.overflow = 'auto';
    };

    window.openViewModal = function(btn) {
        const row = btn.closest('tr.risk-row');
        if (!row) return;
        const json = row.getAttribute('data-risk');
        if (!json) return;
        let risk;
        try { risk = JSON.parse(json); } catch (e) { return; }

        document.getElementById('viewTitle').textContent = risk.Title || '';
        document.getElementById('viewCategory').textContent = risk.Category || '';
        document.getElementById('viewPriority').textContent = risk.Priority || '';
        document.getElementById('viewStatus').textContent = risk.Status || '';
        document.getElementById('viewSourceType').textContent = risk.SourceType || '';
        document.getElementById('viewSiteName').textContent = risk.SiteName || 'Unassigned';
        document.getElementById('viewProjectSite').textContent = risk.ProjectSite || '';
        document.getElementById('viewDateReported').textContent = risk.DateReported || '';
        document.getElementById('viewReportedBy').textContent = risk.ReportedBy || '—';

        const attWrap = document.getElementById('viewAttachmentsWrap');
        const previewEl = document.getElementById('viewAttachmentsPreview');
        const isImage = function (u) {
            const ext = (u || '').split('.').pop()?.toLowerCase();
            return ['jpg', 'jpeg', 'png', 'gif', 'webp'].indexOf(ext || '') >= 0;
        };
        if (risk.Attachments && risk.Attachments.length > 0) {
            attWrap.classList.remove('hidden');
            const imageUrls = risk.Attachments.filter(isImage);
            if (imageUrls.length > 0) {
                previewEl.innerHTML = '<div class="flex flex-wrap items-center justify-center gap-3">' + imageUrls.map(function(url) {
                    return '<img src="' + url + '" alt="Attachment" class="max-h-[220px] max-w-full rounded-lg object-contain shadow-sm" />';
                }).join('') + '</div>';
            } else {
                previewEl.innerHTML = '<div class="flex flex-col items-center gap-2 text-slate-400"><svg class="h-12 w-12" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14" /></svg><span class="text-sm font-medium">File attachment (no preview)</span></div>';
            }
        } else {
            attWrap.classList.remove('hidden');
            previewEl.innerHTML = '<div class="flex flex-col items-center justify-center gap-3 text-slate-400"><div class="flex h-20 w-20 items-center justify-center rounded-xl border-2 border-dashed border-slate-300 dark:border-slate-600"><svg class="h-8 w-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14" /></svg></div><span class="text-xs font-medium">No attachment</span></div>';
        }

        document.getElementById('viewEditRiskId').value = risk.Id;
        document.getElementById('viewEditDate').value = risk.DateReported || '';
        document.getElementById('viewEditCategory').value = risk.Category || 'Operational';
        document.getElementById('viewEditTitle').value = risk.Title || '';
        document.getElementById('viewEditSourceType').value = risk.SourceType || 'Inspection';
        document.getElementById('viewEditStatus').value = risk.Status || '';
        document.getElementById('viewEditReportedBy').value = risk.ReportedBy || '';
        document.getElementById('viewEditSiteId').value = risk.SiteId != null ? risk.SiteId.toString() : '';
        document.getElementById('viewEditProjectSite').value = risk.ProjectSite || '';

        document.getElementById('viewModalSubmitRiskId').value = risk.Id;
        document.getElementById('viewModalAssessLink').href = assessBaseUrl + '/' + risk.Id;

        const status = risk.Status || 'Draft';
        const actionStatus = (status === 'Submitted' || status === 'Reviewed' || status === 'For review') ? 'For_Review' : status;
        // Draft: Submit + Edit only. For Review: Assess + Reject only.
        const canSubmitDraft = !risk.DeletedAt && actionStatus === 'Draft' && (!isEmployeeOnly || risk.ReportByUserId === currentUserId);
        const canEdit = !risk.DeletedAt && actionStatus === 'Draft' && (risk.ReportByUserId === currentUserId || isAdmin);
        const canAssess = !risk.DeletedAt && actionStatus === 'For_Review' && isRiskManager;
        const canReject = !risk.DeletedAt && actionStatus === 'For_Review' && isAdmin;

        const bottomBar = document.getElementById('viewModalBottomBar');
        const submitForm = document.getElementById('viewModalSubmitForm');
        const assessLink = document.getElementById('viewModalAssessLink');
        const rejectBtn = document.getElementById('viewModalRejectBtn');
        const footer = document.getElementById('viewModalFooter');

        if (canSubmitDraft || canEdit || canAssess || canReject) {
            bottomBar.classList.remove('hidden');
            submitForm.classList.toggle('hidden', !canSubmitDraft);
            assessLink.classList.toggle('hidden', !canAssess);
            rejectBtn.classList.toggle('hidden', !canReject);
            footer.classList.toggle('hidden', !canEdit);
        } else {
            bottomBar.classList.add('hidden');
        }

        rejectBtn.onclick = function() {
            toggleViewModal();
            openRejectModal(risk.Id);
        };

        document.querySelectorAll('#viewWorkflowStepper .step').forEach(function(s) {
            s.classList.remove('step-active');
            if (s.getAttribute('data-step') === status || (status === 'For review' && s.getAttribute('data-step') === 'Submitted')) s.classList.add('step-active');
        });

        document.getElementById('viewRiskContent').classList.remove('hidden');
        document.getElementById('viewModalEditPanel').classList.add('hidden');

        document.getElementById('viewRiskModal').classList.remove('hidden');
        document.body.style.overflow = 'hidden';
    };

    window.switchViewModalToEdit = function() {
        document.getElementById('viewRiskContent').classList.add('hidden');
        document.getElementById('viewModalBottomBar').classList.add('hidden');
        document.getElementById('viewModalEditPanel').classList.remove('hidden');
    };

    window.switchViewModalToView = function() {
        document.getElementById('viewModalEditPanel').classList.add('hidden');
        document.getElementById('viewRiskContent').classList.remove('hidden');
        document.getElementById('viewModalBottomBar').classList.remove('hidden');
    };

    window.openRejectModal = function(riskId) {
        document.getElementById('rejectRiskId').value = riskId;
        document.getElementById('rejectRemarks').value = '';
        document.getElementById('rejectModal').classList.remove('hidden');
        document.body.style.overflow = 'hidden';
    };

    window.closeRejectModal = function() {
        document.getElementById('rejectModal').classList.add('hidden');
        document.body.style.overflow = 'auto';
    };

    window.openHardDeleteModal = function(riskId, orgId) {
        document.getElementById('hardDeleteRiskId').value = riskId;
        document.getElementById('hardDeleteOrgId').value = orgId;
        document.getElementById('hardDeleteModal').classList.remove('hidden');
        document.body.style.overflow = 'hidden';
    };

    window.closeHardDeleteModal = function() {
        document.getElementById('hardDeleteModal').classList.add('hidden');
        document.body.style.overflow = 'auto';
    };

    // Auto-fill Job Site Location with user's address via reverse geocoding.
    window.autoDetectLocation = function(inputId, mapId) {
        const input = document.getElementById(inputId);
        const mapEl = document.getElementById(mapId);
        if (!input || !mapEl) return;
        if (!navigator.geolocation) { alert('Geolocation not supported'); return; }
        input.placeholder = 'Detecting location...';
        input.value = '';
        navigator.geolocation.getCurrentPosition(
            function(pos) {
                const lat = pos.coords.latitude;
                const lng = pos.coords.longitude;
                input.placeholder = 'Job Site Location';
                fetch('https://nominatim.openstreetmap.org/reverse?lat=' + lat + '&lon=' + lng + '&format=json', {
                    headers: { 'Accept': 'application/json', 'User-Agent': 'RiskManagementSystem/1.0' }
                })
                    .then(function(r) { return r.json(); })
                    .then(function(data) {
                        if (data && data.display_name) {
                            input.value = data.display_name;
                        } else {
                            input.value = lat.toFixed(6) + ', ' + lng.toFixed(6);
                        }
                    })
                    .catch(function() {
                        input.value = lat.toFixed(6) + ', ' + lng.toFixed(6);
                    });
                if (typeof L !== 'undefined' && mapEl) {
                    if (mapId === 'addMap' && _addMapInstance) { _addMapInstance.remove(); _addMapInstance = null; }
                    if (mapId === 'editMap' && _editMapInstance) { _editMapInstance.remove(); _editMapInstance = null; }
                    const m = L.map(mapId).setView([lat, lng], 15);
                    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png').addTo(m);
                    L.marker([lat, lng]).addTo(m);
                    if (mapId === 'addMap') _addMapInstance = m; else _editMapInstance = m;
                }
            },
            function() {
                input.placeholder = 'Job Site Location';
                alert('Could not get location');
            }
        );
    };
})();
</script>
}